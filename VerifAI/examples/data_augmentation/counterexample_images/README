This folder stores the images of the two counterexample sets. The images with the prefix "random_" are selected by randomly choosing images from the error table. The images with the prefix "kclosest_" are the "k" (=4 in the running example) samples in the error table.
